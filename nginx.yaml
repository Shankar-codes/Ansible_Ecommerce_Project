- name: install nginx using the dynamic inventory plugin for AWS EC2
  hosts: dynamic_frontend
  become: true
  tasks:
    - name: install nginx
      ansible.builtin.dnf:
        name: nginx
        state: present